# 주의: 이 파일을 그대로 .env로 복사하지 말고 `make setup TARGET=...`으로 실제 .env를 생성한다.
# 최초 setup은 ACCESS_PROFILE=local로 Gateway와 Grafana를 loopback에만 공개한다.
APP_ENV=local
ACCESS_PROFILE=local
DEPLOYMENT_TARGET=linux-nvidia-dynamic
BUILD_PROFILE=compose
LOG_LEVEL=INFO
# 기본 꺼짐. true면 chat completions(non-streaming만)/embeddings/risk detector
# 요청·응답 원문을 PII/secret 마스킹(detectors/masking.py) 후
# http_request_completed 로그에 남긴다. 내부 디버깅 전용 — 신뢰되지 않는
# 네트워크로 트래픽이 열리면 다시 false로.
LOG_REQUEST_RESPONSE_BODY=false
# Stable Compose resource namespace. `compose`는 기존 배포에서 쓰일 수 있으므로 개발 기본값으로 사용하지 않는다.
# 같은 Docker host의 다른 개발 clone에는 별도 고유 값을 지정한다.
COMPOSE_PROJECT_NAME=ai-model-serving-platform

# API 문서 UX: Scalar/ReDoc/OpenAPI는 기본 활성화한다.
FASTAPI_DOCS_ENABLED=true
FASTAPI_DOCS_URL=/docs
FASTAPI_REDOC_URL=/redoc
OPENAPI_URL=/openapi.json

# 브라우저 기반 클라이언트(예: 별도 webui)가 다른 origin에서 이 API를 호출할 수 있게 한다.
# 기본값은 전체 허용("*") — local_open의 "네트워크 경계가 접근 제어를 소유한다" 기조와
# vLLM 자체 기본값에 맞췄다. 더 좁히려면 콤마 구분 origin 목록을 명시하거나, 완전히
# 막으려면 빈 값으로 둔다(그러면 CORS 미들웨어 자체를 안 붙인다).
CORS_ALLOWED_ORIGINS=*

# Gateway process listen address. Host publish address is GATEWAY_BIND_ADDR.
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=9400
# Host publish bind for ops/compose/full-stack.private-network.yaml.
# Prefer the 175 internal interface IP in shared/staging environments.
GATEWAY_BIND_ADDR=127.0.0.1
RISK_SIGNAL_SERVICE_HOST=0.0.0.0
RISK_SIGNAL_SERVICE_PORT=9405

# Compose profile: ops/compose/full-stack.private-network.yaml의 service name을 사용한다.
MAIN_MODEL_BASE_URL=http://main-llm-vllm:9401/v1
EMBEDDING_BASE_URL=http://embedding-vllm:9402/v1
EMBEDDING_KO_BASE_URL=http://embedding-ko-vllm:9406/v1
PROMPT_INJECTION_DETECTOR_BASE_URL=http://prompt-injection-detector-runtime:9403/v1
RISK_SIGNAL_SERVICE_BASE_URL=http://risk-signal-service:9405

# Main Model의 client-facing Public Model Alias와 boot/static profile 설정.
MAIN_MODEL_ALIAS=local-main
MAIN_MODEL_BOOT_PROFILE=gemma4-12b-unified-fp8
MAIN_MODEL_PROFILE_LOCKED=false
MAIN_MODEL_STATIC_PROFILE=gemma4-12b-unified-fp8
EMBEDDING_MODEL=local-embed
EMBEDDING_KO_MODEL=local-embed-ko
PROMPT_INJECTION_DETECTOR_MODEL=risk-prompt

REQUEST_TIMEOUT_SECONDS=125
RISK_SIGNAL_SERVICE_TIMEOUT_SECONDS=15
VLLM_TIMEOUT_SECONDS=20
MAIN_MODEL_TIMEOUT_SECONDS=120
EMBEDDING_TIMEOUT_SECONDS=20
EMBEDDING_KO_TIMEOUT_SECONDS=20
PROMPT_INJECTION_DETECTOR_TIMEOUT_SECONDS=5

# Request body limit 기본값은 configs/model_serving.yaml의
# operational_limits.max_request_body_bytes가 소유한다. MAX_REQUEST_BODY_BYTES는
# persistent `.env` key가 아니며 env lifecycle이 제거한다.
# MAIN_MODEL_MAX_CONCURRENCY는 active main-model profile의 실제 vLLM
# --max-num-seqs와 별개로 Gateway 자체 admission 한도를 올린다.
# active profile의 --max-num-seqs보다 크게 잡아도 실제 처리량이 바로 늘지는 않는다.
# 대신 두 번째 이후 요청이 Gateway semaphore에서 queue_timeout_seconds 뒤
# QUEUE_TIMEOUT으로 빠르게 실패하지 않고, vLLM scheduler queue에서
# MAIN_MODEL_TIMEOUT_SECONDS 범위 안에 대기할 수 있게 한다.
# 이 값만으로 GPU memory/compute를 추가 예약하지는 않으며, 대기가 발생하는 위치만 바꾼다.
# Gateway 재시작만으로 되돌릴 수 있고, image rebuild나 main-model profile switch는 필요 없다.
MAIN_MODEL_MAX_CONCURRENCY=4
EMBEDDING_MAX_CONCURRENCY=2
EMBEDDING_KO_MAX_CONCURRENCY=2
PROMPT_INJECTION_DETECTOR_MAX_CONCURRENCY=1
# 장문 요청이 동시에 몰려도 Gateway가 vLLM 대기열에 안정적으로 넘길 수 있게
# main 모델 admission 대기는 90초까지 허용한다. 배포 smoke도
# REQUEST_TIMEOUT_SECONDS를 같은 상한으로 사용한다.
MAIN_MODEL_QUEUE_TIMEOUT_SECONDS=90
MAIN_MODEL_CIRCUIT_BREAKER_FAILURE_THRESHOLD=3
MAIN_MODEL_CIRCUIT_BREAKER_RESET_SECONDS=15
MAIN_MODEL_GPU_MEMORY_UTILIZATION=
# Reference resource policy가 이 host에 맞지 않을 때만 쓰는 명시적 Main Model
# resource-policy override다. 비워 두면 profile의 reference policy로 기동한다.
# 새 GPU 제품명이 생겼다는 이유만으로 설정하지 않는다. id를 명시했다면 해당
# override가 없는 profile로 조용히 fallback하지 않는다. 예: rtx4090-24gb
MAIN_MODEL_RESOURCE_VARIANT=
MAIN_MODEL_SWITCH_IDEMPOTENCY_TTL_SECONDS=
EMBEDDING_QUEUE_TIMEOUT_SECONDS=2
EMBEDDING_KO_QUEUE_TIMEOUT_SECONDS=2
PROMPT_INJECTION_DETECTOR_QUEUE_TIMEOUT_SECONDS=2
PROMPT_INJECTION_DETECTOR_CIRCUIT_BREAKER_FAILURE_THRESHOLD=3
PROMPT_INJECTION_DETECTOR_CIRCUIT_BREAKER_RESET_SECONDS=15
# HTTP client connection 기본값은 configs/model_serving.yaml의
# operational_limits.http_client.max_connections/max_keepalive_connections가 소유한다.
# HTTP_MAX_CONNECTIONS/HTTP_MAX_KEEPALIVE_CONNECTIONS는 persistent `.env` key가 아니며
# env lifecycle이 제거한다.
STARTUP_WAIT_SECONDS=45
READY_FULL_TIMEOUT_SECONDS=1800
READY_FULL_INTERVAL_SECONDS=10

# ACCESS_PROFILE이 resolve한 내부 topology다. 일반 사용자는 직접 조합하지 않는다.
# Advanced/legacy 운영에서만 auth/exposure 도구로 개별 값을 관리한다.
EXPOSURE_MODE=private_network

# EXPOSURE_AUDIENCE: EXPOSURE_MODE=master_open 시 필수. 누구에게 포트가 열리는지 선언한다.
# allowed values: local_only, private_lan, vpn, public
# - local_only: 127.0.0.1 bind; 개발자 로컬 전용
# - private_lan: 사내망/사설 IP bind; staging/ops 전용
# - vpn: VPN 경계 내부에서만 접근 가능
# - public: internet-reachable (ALLOW_PUBLIC_OPERATIONS_ENDPOINTS=true 추가 필요)
# private_network에서는 선택 사항이다.
EXPOSURE_AUDIENCE=local_only

# ALLOW_PUBLIC_OPERATIONS_ENDPOINTS: EXPOSURE_AUDIENCE=public 시 명시적 opt-in이 필요하다.
# true로 설정하면 vLLM raw API 및 ops endpoint가 인터넷에 직접 노출된다.
# ALLOW_PUBLIC_OPERATIONS_ENDPOINTS=

# local access profile은 loopback에서 application 인증 없이 사용한다.
AUTH_MODE=local_open
API_KEY_REQUIRED=false
ADMIN_API_KEY_REQUIRED=false
# /ready와 /metrics를 app-level auth 대신 내부망/ingress에서 보호할 때만 true로 둔다.
ADMIN_ENDPOINTS_INTERNAL_ONLY=false
# non-local internal_trusted는 network/edge/caller 인증 소유권 증거가 필요하다.
INTERNAL_TRUSTED_AUTH_EVIDENCE=
# non-local custom은 명시적 risk acceptance와 change ticket이 필요하다.
CUSTOM_AUTH_RISK_ACCEPTED=false
CUSTOM_AUTH_RISK_TICKET=
ADMIN_API_KEY=generate-with-make-init-env
ADMIN_API_KEYS=generate-with-make-init-env
# Gateway가 허용하는 Bearer key 목록(콤마 구분).
API_KEYS=change-me
INTERNAL_SERVICE_TOKEN=change-me-internal
INTERNAL_SERVICE_AUTH_REQUIRED=false
# smoke/ops client가 사용할 단일 Bearer key. 기본 생성 경로에서는 API_KEYS의 첫 key와 같다.
API_KEY=change-me

# ops/compose/full-stack.private-network.yaml에서 사용한다. `make init-env-compose`가 안전한 key와 검토된 기본 image ref를 채운다.
# project-owned Platform/vLLM은 로컬 build tag에서 시작해 build 시 Docker image ID로 고정한다.
# third-party operations image는 multi-arch registry digest가 기본이다.
PLATFORM_IMAGE=ai-model-serving-platform:0.0.1
# compose-up의 one-shot Runtime Startup directive 재적용을 막는 호환 토큰.
# 일반 운영자가 직접 설정할 값은 아니며 naming은 후속 migration에서 정리한다.
DEPLOY_RELEASE_ID=
# 26B/12B/embedding/embedding-ko/risk-prompt는 VLLM_IMAGE가 가리키는 단일
# qualified runtime artifact를 공유한다. EMBEDDING_KO_VLLM_IMAGE/RISK_VLLM_IMAGE는
# retired persistent key이며 기존 .env에 남아 있으면 env lifecycle이 제거한다.
VLLM_IMAGE=ai-model-serving-vllm-unified:0.0.1
# main-LLM 프로필(configs/main_model_profiles.yaml)의 image override 전용. 위
# VLLM_IMAGE와 같은 이미지의 immutable registry ref 또는 local Docker image ID를 쓸 수 있다.
# local make build는 Docker image ID를 자동 주입한다(직접 수정 불필요). 비어 있으면
# Main Model profile은 공유 image로 fallback하며 profile capability canary가 go-live를 막는다.
MAIN_MODEL_VLLM_IMAGE_OVERRIDE=
DCGM_EXPORTER_IMAGE=nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless@sha256:ed594cf53fe6942e84b07b0740cdcbb249fa4b39cb21feeebf93881ae51f0b5e
PROMETHEUS_IMAGE=prom/prometheus:v3-distroless@sha256:50c707e96da5ade383cb1707790576480485e93de06aa60ad8802cb5f744bd0a
GRAFANA_IMAGE=grafana/grafana:12.2.8@sha256:4fd3611a33dff4ee61e9f319f00ce050d59108c90f5b643f28871728b4542caf
CADVISOR_IMAGE=gcr.io/cadvisor/cadvisor:v0.49.2@sha256:aea169b3b211216d9d61c33455ec3a85d2b6f63d8889b1ebf41b97163cffbbc5
LOKI_IMAGE=grafana/loki:3.3.2@sha256:8af2de1abbdd7aa92b27c9bcc96f0f4140c9096b507c77921ffddf1c6ad6c48f
ALLOY_IMAGE=grafana/alloy:v1.18.0@sha256:491b0578c04983fd54fe99b587b6fab4404dc46d0dc16677bd6b00cc1140b308
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=generate-with-make-init-env
GRAFANA_ANONYMOUS_ENABLED=false
# Loki가 컨테이너 로그를 실제로 보관하는 기간(시간). 짧게 잡아도 충분하면(예: 하루)
# 24로 줄인다 -- 늘려도 이 규모(단일 노드, filesystem storage)에서는 디스크 부담이 크지 않다.
LOKI_RETENTION_HOURS=168

PROMETHEUS_PORT=9410
GRAFANA_PORT=9411
DCGM_EXPORTER_PORT=9412
CADVISOR_PORT=9413
LOKI_PORT=9414
MAIN_MODEL_VLLM_PORT=9401
PROMPT_INJECTION_DETECTOR_RUNTIME_PORT=9403
# ACCESS_PROFILE=local/edge는 loopback, private은 0.0.0.0으로 이 값을 resolve한다.
GRAFANA_BIND_ADDR=127.0.0.1
PROMETHEUS_BIND_ADDR=127.0.0.1
LOKI_BIND_ADDR=127.0.0.1
RISK_SIGNAL_SERVICE_BIND_ADDR=127.0.0.1
DCGM_EXPORTER_BIND_ADDR=127.0.0.1
CADVISOR_BIND_ADDR=127.0.0.1
MAIN_MODEL_VLLM_BIND_ADDR=127.0.0.1
EMBEDDING_VLLM_BIND_ADDR=127.0.0.1
EMBEDDING_KO_VLLM_BIND_ADDR=127.0.0.1
PROMPT_INJECTION_DETECTOR_RUNTIME_BIND_ADDR=127.0.0.1

# gated Hugging Face model을 쓰면 필요하다. `make setup`의 동기화는 기존 운영자 값을 보존한다.
HF_TOKEN=
HUGGING_FACE_HUB_TOKEN=
# vLLM/Hugging Face 다운로드 캐시. compose는 이 host path를 /root/.cache/huggingface로 mount한다.
# 실제 Hub repository는 이 경로의 hub/ 아래에 저장되므로 HF_CACHE_DIR에 hub/를 포함하지 않는다.
# Production deploy resolves relative HF_CACHE_DIR values from the compose file directory.
# With COMPOSE_FILE=ops/compose/full-stack.private-network.yaml,
# ./model_cache/huggingface resolves to <deployment-root>/ops/compose/model_cache/huggingface.
HF_CACHE_DIR=./model_cache/huggingface
