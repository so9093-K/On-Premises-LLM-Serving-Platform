[project]
name = "ai-model-serving-platform"
version = "0.1.0"
description = "Gateway-centered vLLM AI model serving platform with chat, embedding, and risk signal APIs."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.12,<3.14"
dependencies = [
  "fastapi==0.128.2",
  "anyio==4.9.0",
  # 애플리케이션은 HTTP/SSE만 제공한다. reload·WebSocket·dotenv bundle 전체가 아닌
  # production 서버에서 실제 사용하는 event loop와 HTTP parser만 명시한다.
  "uvicorn==0.46.0",
  "uvloop==0.22.1",
  "httptools==0.7.1",
  "httpx==0.28.1",
  "huggingface_hub==1.13.0",
  # 전화번호 탐지. Google libphonenumber의 파이썬 포팅으로, 의존성이 없고
  # 순수 파이썬이라 PII 탐지기의 "외부 모델 없이 결정적" 성질을 유지한다.
  # lite 배포판을 쓰는 이유: 전체 배포판은 geocoder/carrier/timezone 메타데이터
  # 때문에 설치 46MB(그중 geodata 38MB)인데, 우리가 쓰는 건 파싱·유효성 판정뿐이라
  # 5.3MB로 충분하다. import 이름은 그대로 `phonenumbers`다.
  "phonenumberslite==9.0.38",
  "prometheus-client==0.25.0",
  "PyYAML==6.0.3",
  "jsonschema==4.26.0",
]

[dependency-groups]
quality = [
  "Jinja2==3.1.6",
  "openai==2.54.0",
  "pytest==8.4.2",
]

[tool.uv]
# 0.12.11은 검증된 최소 toolchain이다. 같은 0.12 minor의 patch release는 허용하고,
# minor 변경은 lock/install semantics를 다시 qualification한 뒤 별도 변경으로 올린다.
required-version = ">=0.12.11,<0.13"
environments = [
  "sys_platform == 'linux' and platform_machine == 'x86_64'",
  "sys_platform == 'linux' and platform_machine == 'aarch64'",
  "sys_platform == 'darwin' and platform_machine == 'arm64'",
]
required-environments = [
  "sys_platform == 'linux' and platform_machine == 'x86_64'",
  "sys_platform == 'linux' and platform_machine == 'aarch64'",
  "sys_platform == 'darwin' and platform_machine == 'arm64'",
]

[tool.pytest.ini_options]
testpaths = ["tests/unit", "tests/contract"]
python_files = ["test_*.py"]
pythonpath = ["src", "."]
addopts = "-p no:cacheprovider -p no:ddtrace -p no:asyncio --strict-markers"

[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

# Scalar/ReDoc 번들과 generated Control Plane Console은 installed package가
# same-origin으로 직접 서빙하는 runtime asset이다. Console은 Vite output을 한 단계
# assets/에만 두므로 package-data도 그 구조를 명시적으로 포함한다.
[tool.setuptools.package-data]
ai_model_serving = [
  "static/*.js",
  "static/control-plane/*",
  "static/control-plane/assets/*",
]
