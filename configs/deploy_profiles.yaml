# full-stack compose-up이 사용하는 Runtime Startup Profile이다. 명시적으로 profile을 고르지 않으면
# default_profile을 적용하며, deferred runtime은 컨테이너만 만들고 stopped로 기록한다.
default_profile: main_only

profiles:
  # 최초 기동은 Main만 준비한다. non-main Model Runtime은 필요할 때 Admin API로 시작한다.
  main_only:
    deferred_runtimes:
      - embedding
      - embedding_ko
      - prompt_injection_detector

  # Retrieval은 처음부터 제공하고 Prompt Injection Detector는 필요할 때 시작한다.
  # host-specific unavailable runtime은 effective topology가 이 목록에서 걸러낸다.
  retrieval_ready:
    deferred_runtimes:
      - prompt_injection_detector
